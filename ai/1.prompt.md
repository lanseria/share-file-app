# AI

当前项目为[share-file-app]
github地址为 https://github.com/lanseria/share-file-app
已经考虑了下面条件：
核心框架: Nuxt 3 (v3.17.4) + Vue 3
语言: TypeScript
样式方案: Unocss CSS
Auto-imports 机制
有主题模式
有响应式布局
就按我的代码风格写
请不要使用传统的diff + / - 格式。请用一种清晰的文本描述格式来展示代码变更，明确指出‘从哪一行开始’到‘哪一行结束’的旧代码块，应该被替换为新的代码块
下面回答全部使用中文回答

```bash
python3 ./ai/print_project_files.py . "\.(vue|ts|js|json|toml|md)$|Dockerfile$|eslint\.config\.js$" -o ./ai/project_context.txt -e "node_modules,.git,.nuxt,dist,build,public/assets,pnpm-lock.yaml,.vscode,ai"
```

1. 完善广播消息

- 目前缺少广播消息 UI 展示
- 将原来的“发送测试广播消息:”UI移除
- 构建新的组件，放置在ICE 调试组件上方
- 新的组件实现聊天功能，就是左边是别人的消息，右边是自己的消息，有头像，名字，发送时间还有聊天文字用气泡UI框包裹，下方是发送消息输入框

## ---------

重点分析一下这个文件，signaling-server\server.js
现在这个服务端的脚本文件实现了客户端的创建房间等功能

我现在想把这个类似的功能下发到其他前端项目中，比如一个可以远程操作的仪表台，比如我这里做了哪些设置操作，远程的那台也是一样的动作，而且指令是单向的。
需要修改原来的服务端代码吗？如果不需要，给我一个文档，让对方直接知道如何使用websocket连接使用